{
  "name": "@raing3/actions",
  "version": "0.1.0",
  "scripts": {
    "build": "lerna exec --concurrency 4 -- npm run build --silent",
    "build:watch": "lerna exec --concurrency 4 -- npm run build:watch --silent",
    "lint": "lerna exec --concurrency 4 --no-bail -- npm run lint --silent",
    "lint:fix": "lerna exec --concurrency 4 --no-bail -- npm run lint:fix --silent",
    "postinstall": "lerna bootstrap --ci"
  },
  "author": "Richard Ainger",
  "license": "ISC",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/raing3/actions.git"
  },
  "devDependencies": {
    "@raing3/eslint-config": "file:../eslint-config",
    "lerna": "^3.20.2"
  },
  "dependencies": {}
}
